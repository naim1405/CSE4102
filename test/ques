Q1. Consider given code,
 

#include<stdio.h>

int main(){

  char name[] = "Hello";

  printf("%s\n", name);

  return 0;

}

Show output file with extension ".i" generated by C compiler along with Makefile (point penalty for adding extra commands other than necessary).


%token VOID_TYPE NUM POINTER 
%token OP CP OB CB  SEMI ARROW
%token ASSIGN
%token CLASS PUBLIC VIRTUAL STRING
%token ID
 


Q2. Consider the following arithmetic expression statements,


class Animal { public : virtual void speak ( ) = 0 ; } ;

class Dog : public Animal { public : void speak ( ) { printf ( "Woof" ) ; } } ;

Animal * ptr = new Dog ( ) ; ptr -> speak ( ) ;



a) Show a flex file which can tokenize given statements.

b) Show a bison file which can parse given statements.

*
